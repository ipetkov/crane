[package]
name = "strip-version-test"
version = "1.2.3"
edition = "2021"
description = "Test package for version stripping functionality"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tokio-test = "0.4"
